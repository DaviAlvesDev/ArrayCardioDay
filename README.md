# 💪 Array Cardio Day

> Projetos #4 e #7 do curso [JavaScript30](https://javascript30.com) feitos com o objetivo de praticar a manipulação de arrays
***
<p align="center">
  <a href="#-ajustes-realizados">Ajustes</a> •
  <a href="#-tecnologias">Tecnologias</a> •
  <a href="#-como-executar">Como Executar</a> •
  <a href="#-aprendizados">Aprendizados</a> •
  <a href="#️-licença">Licença</a> •
  <a href="#️-créditos">Créditos</a> •
</p>

## 🔧 Ajustes Realizados
- **Adaptação para TypeScript:** Migração do código para TypeScript, reforçando a tipagem dos componentes

## 🛠 Tecnologias
- **HTML5**
- **TypeScript** 

## 🚀 Como Executar
**1. Clonar o repositório:**
```bash
git clone https://github.com/DaviAlvesDev/ArrayCardioDay.git
```
**2. Instalar as dependências:**
```bash
npm install
```
**3. Compilar o código em TypeScript para JavaScript:**
```bash
npm run build
```
**4. Abrir o projeto:** Abra o arquivo `index.html` no seu navegador ou utilize o comando `npm start`
***
🔗 **Se preferir não baixar o projeto:** [acesse-o hospedado no GitHub Pages](https://davialvesdev.github.io/ArrayCardioDay/)

## 📝 Aprendizados
* **Manipulação e Agregação de Dados**
  * Uso de `filter` para extração seletiva e `map` para transformação de dados.
  * Implementação de `reduce` para cálculos complexos e criação de mapas de frequência (contagem de instâncias).
  * Ordenação personalizada com `sort` baseada em propriedades calculadas (ex: idade).

* **Pesquisa e Validação**
  * Predicados lógicos com `some` e `every` para validações rápidas de estado.
  * Localização de elementos e índices com `find` e `findIndex`.

* **Trabalhando com Objetos (Interoperabilidade)**
  * Conversão de objetos em estruturas iteráveis via `Object.entries()`, permitindo o uso de métodos de array em dicionários de dados.
  * Aplicação de **Destructuring** para simplificar o acesso a pares de chave/valor durante a iteração.

* **TypeScript e Segurança de Código**
  * Tipagem de acumuladores dinâmicos utilizando `Record<K, T>`.
  * Uso de **Optional Chaining** (`?.`) para garantir a resiliência do código contra valores `null` ou `undefined` em operações de busca.
## ⚖️ Licença
Este projeto está sob a licença MIT. 

Para mais informações, acesse o arquivo [LICENSE](LICENSE)

## ✍️ Créditos
> Feito com ☕ por [@DaviAlvesDev](https://github.com/DaviAlvesDev)